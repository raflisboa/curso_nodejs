{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}

<h4>Categorias</h4>

{{#each categorias}}
    <a href="/categorias/{{slug}}"><h4>{{nome}}</h4></a>
{{else}}
{{/each}}