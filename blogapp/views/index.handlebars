{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}
<div class="jumbotron">
  <h1 class="display-4">Blog NodeJS</h1>
    <p class="lead">Neque porro quisquam est qui dolorem ipsum quia dolor sit amet, consectetur, adipisci velit...</p>
        <hr class="my-4">
    <p>There is no one who loves pain itself, who seeks after it and wants to have it, simply because it is pain...</p>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie sua conta</a>
</div>
<hr>
<h2>Postagens Recentes:</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}" class="btn btn-info">Leia mais</a>
        <hr>
            <small>Data: {{#formatDate date}}{{/formatDate}}</small><br>
            <small>Categoria: {{categoria.nome}}</small><br>
    </div>
</div>
{{else}}
    <h5> Nenhuma Postagem</h5>
{{/each}}
